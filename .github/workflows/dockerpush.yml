# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

# GitHub recommends pinning actions to a commit SHA.
# To get a newer version, you will need to update the SHA.
# You can also reference a tag or branch, but the action may change without warning.

name: Publish Docker image

on:
  push:
    branches:
      - master

jobs:
  build-images:
    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    env:
      JWT_EXPIRE: 4d
      JWT_SECRET: qwertyuiiuyujsnna
      PORT: 4000
      PORT_1: 8080
      DB_URI: "mongodb://mongo_db:27017"
      DB_NAME: "reservation"
      COOKIE_EXPIRE: 5
      SMPT_SERVICE: gmail
      SMPT_HOST: smtp.gmail.com
      SMPT_PORT: 587
      SMPT_MAIL: oluwaronaldo7@gmail.com
      SMPT_PASSWORD: yhuywfmkieyawbgc
      CLOUDINARY_NAME: ddkil2k9c
      CLOUDINARY_APIKEY: 959645165615747
      CLOUDINARY_SECRET: N0WbbvjSyLI7Ul9Op6PmKrdeyS4
    steps:
      # Log into github registry
      - name: Log into registry
        run: echo "${{ secrets.SECRET_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
      # Check out the repo
      - name: Checkout
        uses: actions/checkout@v2
      # Build the images
      - name: Build the docker-compose stack
        run: docker-compose -f docker-compose.yml -f  build
      # List images
      - name: List images
        run: docker images
      # Push images
      - name: Push images
        run: docker-compose -f docker-compose.yml  push --ignore-push-failures
      - name: Log in to Docker Hub
        uses: docker/login-action@f054a8b539a109f9f41c372932f1ae047eff08c9
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Build and push Docker image
        uses: docker/build-push-action@ad44023a93711e3deb337508980b4b5e9bcdc5dc
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
